# Data221 - Assignment 1

1. Data221Assignment1Question1 
   Multiplies consecutive integers starting from 1 until the product exceeds a threshold.

2. Data221Assignment1Question2  
   Defines a function that takes a list of strings and returns a nested dictionary with string length and parity (even/odd).

3. Data221Assignment1Question3
   Computes powers `x^y` from a list of base–exponent pairs, skipping negative exponents.

4. Data221Assignment1Question4 
   Sorts a list of random values and finds indices where values are greater than or equal to a random threshold.

5. Data221Assignment1Question5
   Compares the areas of two circles, validates input, and returns the percentage coverage of the smaller circle over the larger.

6. Data221Assignment1Question6
   Creates a dictionary showing the cumulative percentage of list elements less than or equal to each unique value.

7. Data221Assignment1Question7 
   Converts a given number of seconds since midnight into hours, minutes, seconds, and AM/PM.

8. Data221Assignment1Question18
   Creates a Pandas DataFrame from given data, adds a derived column, and prints the result.
